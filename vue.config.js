// vue.config.js
module.exports = {
  publicPath: '/',  // 部署到根目录
  outputDir: 'dist', // 构建输出目录

  // 如果你的域名是 herotemple.github.io，用上面的配置
  // 如果你要用其他项目名仓库，如 portfolio，则用下面的：
  // publicPath: '/portfolio/',
}
